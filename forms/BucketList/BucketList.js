/*
myBucketList = []

keepgoing = true

while (keepgoing == true) {
 bucketListChoice = prompt('Do you want to add to your bucketlist (type yes or no)')
  if (bucketListChoice == "yes") {
  addBucketList = prompt('enter a bucketlist item')
  myBucketList.push(addBucketList)
}
  else {
  keepgoing = false
  }
  }
  console.log('Here is your bucketlist')
  for(i = 0; i<=myBucketList.length - 1; i++) {
  console.log(myBucketList[i])
  }
  */